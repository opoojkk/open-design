interface:
  display_name: "HTML to Design Spec"
  short_description: "Convert HTML prototypes into design specs"
  default_prompt: "Use $html-to-design-spec to convert this Open Design or HTML prototype into pages.json, style.json, screenshots/, and optional flows.json, graph.json, prompts/, and specs/ when needed."
